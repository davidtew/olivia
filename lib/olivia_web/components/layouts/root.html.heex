<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="csrf-token" content={get_csrf_token()} />
    <.live_title suffix=" - Olivia Tew">
      {assigns[:page_title] || "Contemporary Painter"}
    </.live_title>

    <meta
      name="description"
      content={
        assigns[:meta_description] ||
          "Contemporary painter Olivia Tew creates bold, expressive works exploring themes of identity, emotion, and the human experience."
      }
    />
    <meta name="author" content="Olivia Tew" />
    <meta name="robots" content="index, follow" />

    <meta property="og:title" content={assigns[:page_title] || "Olivia Tew - Contemporary Painter"} />
    <meta
      property="og:description"
      content={
        assigns[:meta_description] ||
          "Contemporary painter Olivia Tew creates bold, expressive works exploring themes of identity, emotion, and the human experience."
      }
    />
    <meta property="og:type" content="website" />
    <meta property="og:url" content={assigns[:canonical_url] || "https://oliviagraham.com"} />
    <meta
      property="og:image"
      content={assigns[:og_image] || "https://oliviagraham.com/images/og-image.jpg"}
    />

    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={assigns[:page_title] || "Olivia Tew - Contemporary Painter"} />
    <meta
      name="twitter:description"
      content={
        assigns[:meta_description] ||
          "Contemporary painter Olivia Tew creates bold, expressive works exploring themes of identity, emotion, and the human experience."
      }
    />
    <meta
      name="twitter:image"
      content={assigns[:og_image] || "https://oliviagraham.com/images/og-image.jpg"}
    />

    <link rel="canonical" href={assigns[:canonical_url] || "https://oliviagraham.com"} />

    <link phx-track-static rel="stylesheet" href={~p"/assets/css/app.css"} />
    <script defer phx-track-static type="text/javascript" src={~p"/assets/js/app.js"}>
    </script>
    <script>
      (() => {
        const setTheme = (theme) => {
          if (theme === "system") {
            localStorage.removeItem("phx:theme");
            document.documentElement.removeAttribute("data-theme");
          } else {
            localStorage.setItem("phx:theme", theme);
            document.documentElement.setAttribute("data-theme", theme);
          }
        };
        if (!document.documentElement.hasAttribute("data-theme")) {
          setTheme(localStorage.getItem("phx:theme") || "system");
        }
        window.addEventListener("storage", (e) => e.key === "phx:theme" && setTheme(e.newValue || "system"));
        
        window.addEventListener("phx:set-theme", (e) => setTheme(e.target.dataset.phxTheme));
      })();
    </script>
  </head>
  <body class="bg-white antialiased">
    <OliviaWeb.Layouts.navigation current_scope={@current_scope} />
    {@inner_content}
    <OliviaWeb.Layouts.footer />
  </body>
</html>
